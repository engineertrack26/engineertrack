# Simulation findings

Severity: blocker = flow cannot continue · wrong = rule/data wrong · rough = works but reads badly. How to read: entries are ordered blocker → wrong → rough and numbered for reference; every entry names the RPC/table or screen file:line it concerns; the raw calls are in `sim/log/<who>.md`.

1. **[wrong]** ayca-yildiz — link a second mentor (myself) onto Elif, who already has a mentor (Hakan Demir). Expected: refused — a student has one mentor. Got: link_student_by_code succeeded: [{"student_id":"5d9c97c7-b804-4a65-890d-7eb65270f2ca","student_name":"Elif Kaya"}]; internship_people → [{"id":"99a265a1-eb47-4e4c-95d0-beee40653f71","name":"Burak Şahin","company":"Ege Çevre Danışmanlık","endDate":"2026-09-19","mentorId":"3a1e5820-0939-4b03-8060-cde6abcdc056","startDate":"2026-09-13"},{"id":"5d9c97c7-b804-4a65-890d-7eb65270f2ca","name":"Elif Kaya","company":"Marmara Su ve Kanalizasyon İdaresi","endDate":"2026-09-19","mentorId":"3a1e5820-0939-4b03-8060-cde6abcdc056","startDate":"2026-09-13"}]; student_profiles(Elif) → {"id":"5d9c97c7-b804-4a65-890d-7eb65270f2ca","mentor_id":"3a1e5820-0939-4b03-8060-cde6abcdc056"} Pointer: docs/internship-groups-rpcs.sql:163 — \`UPDATE student_profiles SET mentor_id = auth.uid()\` with no existing-mentor guard; Hakan re-linked Elif afterwards (phase 3b, sim/log/hakan-demir.md).
2. **[wrong]** deniz-yildirim — send a message in a conversation I just blocked myself. Expected: refusal BLOCKED (or another stable refusal code). Got: success "86088dab-1632-44a5-af16-050921f7cc71" — block_conversation only records blocker_id=me, and send_message's BLOCKED check tests blocker_id=the OTHER participant, so blocking myself does not stop my own outgoing messages Pointer: docs/direct-messages-rpcs.sql:96-97 (send_message refuses only when \`blocker_id = v_other\`) vs :147 (block_conversation inserts \`blocker_id = auth.uid()\`).
3. **[wrong]** hakan-demir — called review_assignment(approved=true, note "tekrar", level 2) on a submission he had already approved with level 1. Expected: refusal — mentor approval is final (CLAUDE.md, task-assignment spec); at least the note/level must not change after approval. Got: success (null): the second call overwrote mentor_level 1→2 and mentor_note; XP is not re-paid, but the recorded judgement changed silently after the student and advisor could already see it. Submission id 1caded2d-76c7-428e-bd1d-7bb39019af56 — \`select mentor_level, mentor_note from assignment_submissions where id = '1caded2d-76c7-428e-bd1d-7bb39019af56'\` shows 2 / "tekrar"; state.json mentors.hakan-demir.reviewed[0].level still holds the pre-overwrite 1.
4. **[wrong]** auditor — read Ayşe's closure report as the advisor, as Ayşe and as Emre (get_internship_report) and rendered it in src/components/screens/InternshipReportScreen.tsx:113 (MarkdownView). Expected: the title "# Internship report — Ayşe Çelik", the date range "2026-09-13 – 2026-09-19", "—" for empty cells and "·" separators. Got: every non-ASCII literal of the DEPLOYED function is mojibake: title "# Internship report ÔÇö Ayşe Çelik", "Internship dates | 2026-09-13 ÔÇô 2026-09-19", empty cells "ÔÇö", journal line "Submitted journals: 6 ┬À Support levels: observed 0 ┬À heavy 3 …" (bytes c3 94 c3 87 c3 b6 = UTF-8 of CP437-decoded e2 80 94). docs/internship-closure-migration.sql:99 has the correct em dash, so the function body was applied through the SQL editor with the wrong encoding; data values (Ayşe Çelik, Güz 2026) are fine. Identical for all three readers, version 2 (`get_internship_report`).
5. **[wrong]** auditor — read the "Attendance decided" notifications every student received after their mentor marked the week (internship_review → internship_notify), rendered on app/(student)/notifications.tsx via notificationContent(). Expected: the range to be the earliest and latest decided day, e.g. "6 day(s) marked present (2026-09-14 to 2026-09-19)". Got: Mert "6 day(s) marked present (2026-09-17 to 2026-09-14)", Ayşe "(2026-09-19 to 2026-09-16)", Elif "(2026-09-15 to 2026-09-18)" for six days, Can "5 day(s) … (2026-09-17 to 2026-09-15)", Deniz "(2026-09-18 to 2026-09-15)" — 5 of 7 students; docs/internship-closure-guards.sql:645 loops `ORDER BY value->>'id'` (uuid order) and takes the first/last day_date of that order instead of min/max, so the Turkish line reads "6 gün için katılım durumu: katıldı (2026-09-17 – 2026-09-14)" (`internship_review`).
6. **[rough]** selin-aydin — drafted a level-4 task for a competency whose group target is level 2. Expected: a guardrail (refusal or warning) — an advisor should not assign above the level the group is working towards. Got: accepted: the NOT_IN_SCOPE trigger and publish_assignments check only that the competency is targeted, never the level (matches the 2026-08-20 spec, which promises no level check) (`NOT_IN_SCOPE`).
7. **[rough]** burak-sahin — submit student_id '21023456 ' (trailing space) and read student_profiles back. Expected: trimmed to '21023456', or at least not stored/shown with a stray trailing space. Got: stored verbatim as '21023456 '.
8. **[rough]** deniz-yildirim — fill the internship form with internship_end_date before internship_start_date. Expected: refusal or a validation error (an internship cannot end before it starts). Got: accepted silently — row written with start=2026-09-19, end=2026-09-13; a later internship_open_day call would raise ID_SETUP per docs/internship-days-migration.sql (sp.internship_end_date < sp.internship_start_date), but nothing at write time stops it.
9. **[rough]** zeynep-arslan, murat-koc — submit a task with no photo and no document (Zeynep, at submission time) and separately approve that same submission with no evidence attached (Murat, at review time) — same submission id 5bf81166-faa5-4fa4-944a-a766b7da1900, two chokepoints. Expected: a refusal — the task form says evidence: at least one photo — at submit time, or at least a refusal/warning at review time. Got: accepted silently at both: submit_assignment stored it with zero photos and zero documents, and review_assignment then approved it with zero photos and zero documents still attached.
10. **[rough]** burak-sahin — submit 5 task(s) with the same one-word reflection "Yaptım." (self level 2, one photo each, no document). Expected: the app to require more than a bare non-empty string for a reflection — or at least surface a soft warning — since REFLECTION_REQUIRED only guards emptiness, not substance. Got: accepted silently on every submission, identical text and all, with no minimum length or word-count enforced anywhere in the flow.
11. **[rough]** elif-kaya, ayse-celik, can-dogan, mert-yilmaz — after their mentors approved 3–4 tasks each, no `task_approved` notification existed (only level_up / badge / internship_* / direct_message). Expected: one notification per decision. Got: none — `review_assignment` does not create it; the mentor's app inserts the notification client-side after the RPC returns (`src/services/mentorReviews.ts`), so any other client, or a crash between the two calls, leaves the student unnotified. Every other event (stream, messages, attendance, closure) notifies in-transaction.
12. **[rough]** auditor — read the advisor attendance table (internship_group_attendance) next to app/(advisor)/reports.tsx:362-364 and the report's Attendance section. Expected: recorded ≤ working days, and "without a record" to equal the weekdays that have no record. Got: six students show "6 of 5 working days recorded so far · 5 in the whole internship" and Ayşe's report says "Working days so far 5 | Recorded 6"; Burak shows "3 of 5 … 2 working days without a record" although three weekdays (14, 15, 16 Sep) have no record — his Saturday 19 Sep record offsets one. internship_open_day accepts any date inside the placement (weekends included) while expectedSoFar/unrecorded count isodow<6 only (docs/internship-days-migration.sql:371,405) (`internship_group_attendance`).
13. **[rough]** auditor — compare the student dashboard "This week" strip (app/(student)/dashboard.tsx:80-83, 218-227: a ✓ only when check_in_at is set) with the internship-days screen for the same week. Expected: a day the mentor marked present to show as present on the dashboard too. Got: 32 of the 39 recorded days were declared late with a reason (check_in_at NULL; only 19 Sep was a live check-in), so Elif/Zeynep/Mert/Ayşe/Can see Mon–Fri as "·" (a11y "—") and a single ✓ on Sat, while the days screen and the mentor show six days present. The strip reads "attendance" but only shows check-ins; the two screens disagree 1 vs 6.
14. **[rough]** auditor — read each student's Competencies tab (app/(student)/achievements.tsx:149) and the advisor Reports tab (reports.tsx:340-342) with 3–4 approved tasks per student. Expected: a competency with an approved, mentor-rated task not to be labelled as untouched. Got: every competency card says "Başlanmadı" (student.competencyNotStarted, currentLevel === 0) directly above a "Sen 2.0 / Mentor 2.0" bar from the approved task in that competency; the advisor sees "Progress · 0%" for all 7 students and the dashboard "0 of 6 at target" after 26 approvals. Level 1 needs 2 observations × 2 KPIs and each task is one observation (spec), so the number is right but the label "not started" is wrong for a competency with approved work.
15. **[rough]** auditor — read my-tasks / task-detail / the stream on a Turkish account after the advisor edited a draft title to "… (revize)" in phase 1 (assignment 45e71428-169c-44fc-b055-e5f5ea417dca; state.json still holds the pre-edit title without "(revize)"). Expected: either the edited title in Turkish or all six titles in one language. Got: five titles render in Turkish and one in English — "Set a logical workflow diagram by dragging and dropping … under mentor guidance. (revize)" (src/components/student/StudentUI.tsx:107, FeedPostCard.tsx:176, review-detail.tsx:188), while its objective and criterion on the same card still translate (task-detail.tsx:282,289): taskContent() matches whole fields only (src/utils/taskContent.ts:20-23, by design), so any suffix drops the whole catalogue entry and the card is bilingual.
16. **[rough]** auditor — read Burak's resubmitted task (assignment 1591a485, submission 6eb16356: needs_revision → resubmitted, now status "submitted") on app/(student)/task-detail.tsx:276-278. Expected: the revision note to be shown as history or cleared once the student resubmitted. Got: the row still carries mentor_note "Kriter ölçüm belirsizliğini istiyor; tabloda yok. Ekleyip yeniden gönder." with mentor_level NULL, so the student's "waiting" card shows "Mentor notu: … Ekleyip yeniden gönder." as if the fix were still outstanding; submit_assignment does not clear mentor_note on resubmission (`submit_assignment`).
17. **[rough]** auditor — read the internship-days screen header as Ayşe/Burak and as their mentors (src/components/internship/InternshipDaysScreen.tsx:246 and :297). Expected: the placement period in the account locale like the day labels next to it. Got: the period is the raw ISO pair "2026-09-13 — 2026-09-19" while each day card is localized by dateLabel() (:107, e.g. "Pzt 14 Eyl"); the closure report uses the same ISO form and "Closed | 2026-09-20 13:22 UTC" on a Turkish account.

## Removed as non-findings

- Phase 5 (four entries): mentors are outside the stream by design (no feed screen in `app/(mentor)/`; `list_feed_posts` guard is owner-or-member per the 2026-09-11 stream spec), and journal correction requests belong to the advisor, not the mentor (`internship_note` guard, days screen) — not app defects.
- Phase 6: 11 phase-6 lines removed as script errors — Mert's `overRated` ×4 (a count, not a boolean), Elif's leaderboard id mismatch ×1, Can's first-page-only stream reads ×5, plus one duplicate notification entry — and four per-person `task_approved` entries merged into #11.
- Phase 8 (fourteen entries): the auditor logged "select another person's internship_days → permission denied" as a defect on all fourteen cross-checks, but that refusal is the correct isolation outcome — the internship-days tables grant no client privileges; access is RPC-only by design. The probe was written with the wrong polarity.
